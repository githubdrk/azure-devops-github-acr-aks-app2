apiVersion: v1
kind: Service
metadata:
    name: githubdrkazuredevopsgithubacraksapp
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: githubdrkazuredevopsgithubacraksapp